name: Test 

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - lwj/test_on_win
  pull_request:
    branches:
      - main

jobs:
  Win_Test:
    runs-on: windows-latest
    strategy:
      matrix:
        python-version: [3.10.9]

    steps:
      - name: test command
        run: |
          dir
          whoami
          ls C:\Users\
          mkdir -p /c/Users/runneradmin/.cache/modelscope/hub
