pull_request_rules:
  - name: Add label after CI passes
    description: Automatically add the 'lint_pass' label after the CI workflow 'Lint And Build Package' passes.
    conditions:
      # 条件：PR必须满足以下条件
      - check-success=Lint And Build Package
    actions:
      label:
        add:
          - lint_pass
